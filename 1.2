def sumsquare (list):
    odd=0
    even=0
    for num in list:
        if num%2==0:
            even+=num**2
        else:
                odd+=num**2
                return[odd,even]
 n=int(input("enter the terms:"))
 if n<=o:
      print("enter a positive terms:")
  for i in range(o,n):
      x=int(input("enter:"))
      list1.append(x)
  print(list1)
  print(sumsquare(list1))
